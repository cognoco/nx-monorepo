# Memory Sync Backlog

Log ByteRover mirror failures here. Each row represents a manifest chunk that could not be uploaded during the synchronous write flow described in `memory-system-architecture.md`.

| chunk_id | manifest | reason | recorded_at | recorded_by |
|----------|----------|--------|-------------|-------------|
<!-- Append new rows when ByteRover sync fails or MCP access is unavailable. Timestamps should be ISO 8601 in UTC. -->

