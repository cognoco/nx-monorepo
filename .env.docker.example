# =============================================================================
# Docker Environment Variables Example
# =============================================================================
# Copy this file to .env.docker and fill in your values.
# Used by: docker compose up (reads from .env.docker by default if configured)
#
# Note: Do NOT commit .env.docker with real values to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Supabase PostgreSQL connection string
# Format: postgresql://postgres.[project-ref]:[password]@[host]:6543/postgres?pgbouncer=true
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-0-YOUR-REGION.pooler.supabase.com:6543/postgres?pgbouncer=true

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Your Supabase project URL
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Supabase anonymous/public key (safe to expose in client-side code)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_ANON_KEY

# -----------------------------------------------------------------------------
# Sentry Configuration (Optional)
# -----------------------------------------------------------------------------
# Leave empty to disable Sentry error tracking
SENTRY_DSN=
SENTRY_ENVIRONMENT=docker

# -----------------------------------------------------------------------------
# Build-Time Variables (Web App)
# -----------------------------------------------------------------------------
# These are baked into the Next.js client bundle at build time.
# In docker-compose.yml, these are passed as build args.
#
# For local Docker Compose, the API URL is the container name:
# NEXT_PUBLIC_API_URL=http://server:4000
#
# For production deployment, use your actual staging/production URL:
# NEXT_PUBLIC_API_URL=https://api.your-domain.com
