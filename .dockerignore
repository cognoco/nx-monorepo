# =============================================================================
# Docker Ignore - Exclude files from build context
# =============================================================================
# This file reduces build context size and prevents sensitive files from
# being included in Docker images.
# =============================================================================

# Dependencies (will be installed fresh in container)
node_modules
**/node_modules

# Build outputs (will be generated in container)
dist
**/dist
# Exception: OpenAPI spec needed for web build (api-client type generation)
!dist/apps/server/openapi.json
.next
**/.next
out
**/out

# Development and test files
**/*.spec.ts
**/*.spec.tsx
**/*.test.ts
**/*.test.tsx
**/__tests__
**/__mocks__
**/jest.config.*
**/jest.setup.*
**/.spec.swcrc
coverage
**/coverage

# E2E tests
apps/web-e2e
**/playwright-report
**/test-results

# Environment files (sensitive - inject via runtime env vars)
.env
.env.*
!.env.example
!.env.docker.example

# Git
.git
.gitignore
.gitattributes

# IDE and editor
.idea
.vscode
*.swp
*.swo
.DS_Store

# Documentation (not needed in production image)
docs
*.md
!README.md
LICENSE

# CI/CD configuration
.github
.husky
.coderabbit.yaml

# Nx cache and local files
.nx
.angular
tmp
nx-cloud.env

# Development tools
.eslintrc*
.prettierrc*
.prettierignore
.editorconfig
.spectral.yaml
coderabbit.yaml

# Mobile app (not part of server/web containers)
apps/mobile

# BMAD and tooling
.bmad
.claude
.ruler
.serena
tools

# Prisma migrations (database schema managed separately)
# Keep schema.prisma for client generation
**/migrations

# Test utilities package (dev only)
packages/test-utils

# TypeScript build info
**/*.tsbuildinfo

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
