# API Configuration
# By default, the app uses Next.js rewrites to proxy /api/* to http://localhost:4000/api/*
# Set this variable to override the API URL (e.g., for production or custom environments)
NEXT_PUBLIC_API_URL=/api
